export interface Planet {
    name: string,
    distance: number
}

export interface Vehicle {
    name: string,
    total_no: number,
    max_distance: number,
    speed: number
}

export interface TokenResponsePayload {
    token: string
}
